<h1>All The Games</h1>

<table>
  <thead>
    <tr>
      <th>#</th>
      <th>Name</th>
      <th>Rating</th>
      <th></th>
  </thead>

  <tbody>
    <% @games.each do |game| %>
    <tr>
      <td><%= game.id %></td>
      <td><%= game.name %></td>
      <td><%= game.rating %></td>
      <td><%= link_to "Show", game %></td>
    </tr>
    <% end %>
  </tbody>
</table>
