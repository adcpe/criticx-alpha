<h1>All the Companies</h1>

<table>
  <thead>
    <tr>
      <th>#</th>
      <th>Name</th>
      <th>Country</th>
      <th></th>
  </thead>

  <tbody>
    <% @companies.each do |company| %>
    <tr>
      <td><%= company.id %></td>
      <td><%= company.name %></td>
      <td><%= company.country %></td>
      <td><%= link_to "Show", company %></td>
    </tr>
    <% end %>
  </tbody>
</table>
