<h1><%= @company.name %></h1>

<p><%= @company.description %></h1>

<ul>
  <li>Start date: <%= @company.start_date %></li>
  <li>Country: <%= @company.country %></li>
</ul>

<%= link_to "Back to Companies", companies_path %>
<%= link_to "Delete this Company", companies_path(company), method: :delete, data: { confirm: "Do you really want to delete this company?" } %>
